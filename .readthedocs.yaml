version: 2
build:
  os: ubuntu-lts-latest
  tools:
    python: "3.14"
    apt_packages:
      - gcc
      - cmake
python:
   install:
      - method: uv
        command: sync
        groups:
           - docs
sphinx:
  builder: html
  configuration: docs/sources/conf.py
  fail_on_warning: false
